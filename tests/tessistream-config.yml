default:
  tessilake:
    depths:
      deep:
        path: !expr file.path(tempdir(),"deep")
      shallow:
        path: !expr file.path(tempdir(),"shallow")
    tessitura: "tessitura"

  tessistream:
    survey_dir: !expr rprojroot::find_testthat_root_file("survey_data")
