default:
  tessilake:
    depths:
      deep:
        path: !expr file.path(tempdir(),"deep")
      shallow:
        path: !expr file.path(tempdir(),"shallow")
    tessitura: "tessitura"

  tessistream:
    survey_dir: !expr here::here("tests/testthat/survey_data")
